export class RegisterUser
{
  id;
  firstName;
  name;
  email;
  password;

  constructor(id, firstName, name, email, password)
  {
    this.id = id;
    this.firstName = firstName;
    this.name = name;
    this.email = email;
    this.password = password;
  }
}
