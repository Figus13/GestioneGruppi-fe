export class StudentWithStatus {
  name;
  firstName;
  id;
  status;

  constructor(id, name, firstName, status) {
    this.name = name;
    this.firstName = firstName;
    this.id = id;
    this.status = status;
  }
}
